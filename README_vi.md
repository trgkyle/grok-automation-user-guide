[![Tải tại đây](https://img.shields.io/badge/⬇_Tải-Tại_Đây-success?style=for-the-badge)](https://chromewebstore.google.com/detail/grok-automation-grokcom-a/kpeloeongamilgpjaibcdmldenfmdngp?authuser=2&hl=vi)

# 🚀 Grok Automation v2.4.8 - Tự động hóa Grok.com AI [![English](https://img.shields.io/badge/English-blue)](README.md) [![中文](https://img.shields.io/badge/中文-red)](README_zh.md)

**Grok Automation** là một công cụ năng suất mạnh mẽ được thiết kế để tăng cường quy trình sáng tạo của bạn trên grok.com của xAI. Dừng việc nhập từng prompt một cách thủ công—hãy tự động hóa quy trình và tạo nội dung chất lượng cao ở quy mô lớn.

-----

## ✨ Các tính năng chính

* **🚀 Xử lý hàng loạt nâng cao:** Xếp hàng hàng chục hoặc hàng trăm prompt và để tiện ích tự động xử lý việc gửi và tạo nội dung.
* **📊 Nhập bảng tính thông minh:** Nhập prompt từ file `.txt` hoặc tệp bảng tính (`.xlsx`, `.csv`) với tính năng chọn sheet/cột và xem trước trực quan.
* **🎬 Tự động hóa Văn bản thành Video:** Tạo video tuyệt đẹp từ mô tả văn bản. Hỗ trợ xử lý hàng loạt với thời gian chờ tùy chỉnh.
* **🖼️ Khung hình thành Video:** Sử dụng hình ảnh tĩnh và thêm các hiệu ứng chuyển động tự động để tạo video động.
* **🧩 Thành phần thành Video:** Tạo hoạt ảnh từ nhiều thành phần giao diện hoặc ảnh nhân vật thành video.
* **🖼️ Tạo hàng loạt Văn bản thành Hình ảnh:** Tạo nhiều hình ảnh đồng thời với hỗ trợ nhiều tỷ lệ khung hình (16:9, 9:16, 1:1, 2:3, 3:2).
* **🔄 Hình ảnh thành Hình ảnh:** Biến đổi và nâng cao hình ảnh hiện có bằng AI với gợi ý văn bản.
* **⚙️ Bộ điều khiển chuyên nghiệp:**
    * **Prompt đồng thời:** Xử lý nhiều prompt cùng lúc để tiết kiệm thời gian.
    * **Chờ thông minh (Smart Delays):** Thiết lập khoảng thời gian tùy chỉnh giữa các prompt để quản lý giới hạn tốc độ hiệu quả.
    * **Tự động Nâng cấp & Tải xuống:** Tự động kích hoạt nâng cấp (upscale) và lưu kết quả cuối cùng vào máy tính của bạn.
    * **Tự động đổi tên tệp:** Tự động đổi tên các tệp đã tải xuống để giữ chúng ngăn nắp.
    * **Lưu vào thư mục:** Chỉ định thư mục con tùy chỉnh cho các tệp tải xuống theo từng dự án.
* **🎭 Tự động thêm ảnh nhân vật:** Tự động ghép và đính kèm ảnh vào prompt dựa trên tên nhân vật trong tên tệp.
* **📊 Quản lý hàng đợi thời gian thực:** Theo dõi tiến trình tạo của bạn với thanh trạng thái trực quan và danh sách prompt hoạt động trong Bảng bên (Side Panel).
* **📂 Quản lý tệp ngăn nắp:** Tự động sắp xếp các bản tải xuống vào các thư mục riêng theo dự án để giữ cho không gian làm việc của bạn sạch sẽ.
* **🔧 Sửa lỗi nhanh:** Công cụ một chạm để khắc phục lỗi tạo video hoặc hình ảnh.
* **💳 Quản lý gói:** Đăng nhập để kiểm tra gói của bạn và theo dõi lượng prompt sử dụng hàng ngày.
* **🌐 Hỗ trợ đa ngôn ngữ:** Hỗ trợ 20 ngôn ngữ (Tiếng Anh, Tiếng Việt, Tiếng Trung, Tiếng Hàn, Tiếng Tây Ban Nha, Tiếng Nhật, Tiếng Pháp, Tiếng Đức, Tiếng Bồ Đào Nha, Tiếng Ả Rập, Tiếng Hindi, Tiếng Indonesia, Tiếng Ý, Tiếng Hà Lan, Tiếng Nga, Tiếng Thái, Tiếng Tagalog (Filipino), Tiếng Thổ Nhĩ Kỳ, Tiếng Urdu và Tiếng Bengali).

-----

## 📥 Cài đặt

### Cách 1: Cửa hàng Chrome trực tuyến (Khuyên dùng)
1. Truy cập [Cửa hàng Chrome trực tuyến](https://chromewebstore.google.com/detail/grok-automation-grokcom-a/kpeloeongamilgpjaibcdmldenfmdngp?authuser=2&hl=vi)
2. Nhấn **Thêm vào Chrome**.

---

## 📖 Hướng dẫn sử dụng

### Bắt đầu

1. **Truy cập Grok.com**
   - Mở [grok.com/imagine](https://grok.com/imagine)
   - Tiện ích chỉ hoạt động trên các trang "Imagine" của Grok.

2. **Mở tiện ích**
   - Nhấn vào biểu tượng tiện ích trên thanh công cụ Chrome. Hãy ghim nó để truy cập dễ dàng hơn!

3. **Cấu hình Cài đặt Hàng loạt**
   - Trong tab **Điều khiển**, bạn có thể thiết lập:
     - **Prompt đồng thời:** Số lượng prompt chạy cùng một lúc.
     - **Thời gian chờ prompt:** Thời gian nghỉ giữa mỗi lần gửi prompt.
     - **Lưu vào thư mục:** Tên thư mục con cho các tệp tải xuống.
     - **Tự động đổi tên tệp:** Bật/tắt tự động đổi tên tệp.

4. **Chọn một chế độ**
   - Chọn từ các chế độ tạo có sẵn: **Văn bản thành Video**, **Khung hình thành Video**, **Thành phần thành Video**, **Văn bản thành Hình ảnh**, hoặc **Hình ảnh thành Hình ảnh**.

---

### 1. Chế độ Văn bản thành Video

1. Chọn chế độ **Văn bản thành Video**.
2. Nhập các prompt vào ô nhập liệu (tách mỗi prompt bằng một **dòng trống**).
3. Hoặc nhấn vào biểu tượng **Tải lên** để nhập prompt:
   - **Tệp văn bản (`.txt`):** Tải trực tiếp danh sách prompt.
   - **Bảng tính (`.xlsx`, `.csv`):** Mở một hộp thoại nhập liệu. Chọn tab bảng tính và cột prompt (tiện ích tự động gợi ý cột chứa nhiều dữ liệu nhất). Bạn có thể xem trước 6 hàng đầu và thấy tổng số hàng trước khi nhập.
4. Nhấn **Chạy** để bắt đầu xử lý hàng loạt.

**Ví dụ Prompt:**
```
Một thành phố cyberpunk tương lai với ánh đèn neon phản chiếu trong mưa.
Camera lướt qua các con hẻm hẹp.

Một khu vườn Nhật Bản yên bình với hoa anh đào rơi xuống ao.
Camera zoom chậm vào những con cá koi đang bơi bên dưới.
```

---

### 2. Chế độ Khung hình thành Video

1. Chọn chế độ **Khung hình thành Video**.
2. Nhấn để tải lên hoặc kéo & thả các hình ảnh nguồn.
3. Nhập các prompt (tách bằng dòng trống). Mỗi hình ảnh sẽ được xử lý với mỗi prompt.
4. Trong **Cài đặt**, chọn **Tùy chọn xử lý ảnh**:
   - *Chỉ dùng khung hình đầu* — một ảnh cho mỗi prompt.
   - *Dùng khung hình đầu và khung hình cuối* — hai ảnh cho mỗi prompt.
5. Nhấn **Chạy**.

---

### 3. Chế độ Thành phần thành Video

1. Chọn chế độ **Thành phần thành Video**.
2. Tải lên các ảnh thành phần hoặc ảnh nhân vật bạn muốn tạo hoạt ảnh.
3. Nhập các prompt (tách bằng dòng trống).
4. Tùy chọn bật **Tự động thêm ảnh nhân vật** để tự động đính kèm ảnh có tên tệp khớp với tên nhân vật trong prompt.
5. Trong **Cài đặt**, đặt **Số ảnh đầu vào tối đa mỗi Prompt** (1–10) để kiểm soát số lượng ảnh dùng cho mỗi prompt.
6. Nhấn **Chạy**.

---

### 4. Chế độ Văn bản thành Hình ảnh

1. Chọn chế độ **Văn bản thành Hình ảnh**.
2. Nhập mô tả chi tiết cho hình ảnh của bạn.
3. Cấu hình **Tỷ lệ khung hình** và **Mô hình hình ảnh** mong muốn trong tab Cài đặt.
4. Nhấn **Chạy**.

---

### 5. Chế độ Hình ảnh thành Hình ảnh

1. Chọn chế độ **Hình ảnh thành Hình ảnh**.
2. Tải lên các ảnh nguồn bạn muốn biến đổi.
3. Nhập các prompt (tách bằng dòng trống).
4. Tùy chọn bật **Tự động thêm ảnh nhân vật** để tự động đính kèm ảnh dựa trên tên tệp.
5. Trong **Cài đặt**, đặt **Số ảnh đầu vào tối đa mỗi Prompt** (1–10).
6. Nhấn **Chạy**.

---

## ⚙️ Cấu hình Cài đặt

Truy cập tab **Cài đặt** để tùy chỉnh trải nghiệm của bạn:

| Cài đặt | Mô tả |
| :--- | :--- |
| **Chế độ mặc định** | Thiết lập chế độ nào sẽ được mở mặc định. |
| **Tỷ lệ khung hình mặc định** | Chọn từ 16:9, 9:16, 1:1, 2:3, hoặc 3:2. |
| **Số đầu ra mỗi Prompt (Video)** | Thiết lập số lượng video (1–4) tạo ra cho mỗi prompt. |
| **Số ảnh đầu ra mỗi Prompt** | Thiết lập số lượng hình ảnh (1–50) tạo ra cho mỗi prompt. |
| **Prompt đồng thời** | Số lượng prompt xử lý cùng một lúc (1–6). |
| **Độ trễ ngẫu nhiên** | Thời gian chờ ngẫu nhiên trước khi xử lý prompt tiếp theo. |
| **Mô hình Hình ảnh** | Chọn mô hình AI: **Tốc độ** hoặc **Chất lượng**. Lưu ý: Chọn "Chất lượng" sẽ giới hạn số lượng ảnh đầu ra tối đa mỗi prompt là 4. |
| **Tùy chọn Video mặc định** | Thời lượng mặc định: **6 giây**, **10 giây**, **6 giây (nối tiếp)** hoặc **10 giây (nối tiếp)**. Tùy chọn nối tiếp giúp ghép nối các prompt thành một video dài (prompt cuối luôn sử dụng tùy chọn không nối tiếp). |
| **Tùy chọn Chế độ Hình ảnh mặc định** | Chế độ đầu vào mặc định cho các prompt hình ảnh: **Ảnh mới** hoặc **Chỉnh sửa ảnh** (sử dụng đầu ra từ prompt trước). |
| **Tùy chọn xử lý ảnh** | Dành cho Khung hình thành Video: chỉ dùng khung hình đầu, hoặc dùng cả khung hình đầu và cuối. |
| **Số ảnh đầu vào tối đa mỗi Prompt** | Cho Thành phần thành Video / Hình ảnh thành Hình ảnh: 1–10 ảnh. |
| **Số lần thử lại tối đa khi lỗi** | Số lần thử lại nếu thất bại (1–20). |
| **Chất lượng tự động tải xuống (Video)** | Không tải xuống, 480p, 480p (Nâng chất lượng), hoặc 720p. |
| **Chất lượng tự động tải xuống (Hình ảnh)** | Không tải xuống hoặc 1k. |
| **Ngôn ngữ** | Chuyển đổi giữa 20 ngôn ngữ (English, Tiếng Việt, 中文, 한국어, Español, 日本語, Français, Deutsch, Português, العربية, हिंदी, Bahasa Indonesia, Italiano, Nederlands, Русский, ภาษาไทย, Filipino, Türkçe, اردو, বাংলা). |

---

## 💡 Mẹo & Thực hành tốt nhất

1. **Thời gian chờ:** Nếu bạn gặp giới hạn tốc độ, hãy tăng **Độ trễ ngẫu nhiên** trong Cài đặt.
2. **Chạy đồng thời:** Hãy bắt đầu với 1 prompt đồng thời và tăng dần để xem tài khoản của bạn hỗ trợ mức nào.
3. **Viết Prompt:** Hãy cụ thể! Prompt chi tiết sẽ dẫn đến kết quả AI tốt hơn. Tách nhiều prompt bằng một dòng trống rõ ràng.
4. **Quản lý tệp:** Dùng trường **Lưu vào thư mục** để giữ các tải xuống theo dự án trong thư mục con có tên.
5. **Ảnh nhân vật:** Đặt tên tệp ảnh theo tên nhân vật (vd: `hero.png`, `villain.jpg`) và bật **Tự động thêm ảnh nhân vật** để chúng được ghép tự động.
6. **Thời lượng video:** Dùng tùy chọn *6 giây (nối tiếp)* hoặc *10 giây (nối tiếp)* để ghép các prompt thành một video dài. Prompt cuối luôn dùng thời lượng không nối tiếp.
7. **Sửa lỗi nhanh:** Nếu quá trình tạo bị kẹt hoặc lỗi, hãy nhấn **Sửa lỗi** trong tab Điều khiển.

---

## 🔧 Khắc phục sự cố

| Vấn đề | Giải pháp |
| :--- | :--- |
| **Tiện ích không hoạt động** | Đảm bảo bạn đang ở [grok.com/imagine](https://grok.com/imagine). Tải lại trang nếu cần. |
| **Lỗi khi tạo** | Grok có thể đang bận. Tiện ích sẽ tự động thử lại theo cài đặt **Số lần thử lại tối đa**. Dùng **Sửa lỗi** để khắc phục thủ công. |
| **Tải xuống không hoạt động** | Đảm bảo tùy chọn "Hỏi nơi lưu từng tệp trước khi tải xuống" đã được **TẮT** trong Cài đặt Chrome. |
| **Yêu cầu đăng nhập** | Hãy đảm bảo bạn đã đăng nhập vào tài khoản Grok.com của mình. |
| **Không đủ ảnh** | Trong chế độ Khung hình thành Video, hãy đảm bảo bạn đã tải lên đủ ảnh cho số lượng prompt. |

---

## 🔒 Quyền riêng tư & Dữ liệu

* **Xử lý tại chỗ:** Tất cả logic tự động hóa chạy cục bộ trong trình duyệt của bạn.
* **Không thu thập dữ liệu:** Chúng tôi không lưu trữ hoặc thu thập các prompt, hình ảnh hoặc dữ liệu tài khoản của bạn.
* **Lưu trữ an sau:** Các cài đặt được lưu trong bộ nhớ cục bộ của trình duyệt và đồng bộ trên tất cả các tab.

---

## 📞 Hỗ trợ

- **Tác giả:** Trường Nguyễn
- **Website:** [kylenguyen.me](https://kylenguyen.me)
- **Discord:** Tham gia cộng đồng của chúng tôi để được hỗ trợ và cập nhật.
- **Phản hồi:** Sử dụng liên kết "Báo lỗi" trong tiện ích.

---

## 📦 Phiên bản

Phiên bản hiện tại: **2.4.8**

---

## 📜 Bản quyền

Bản quyền © 2026 **Trường Nguyễn**. Bảo lưu mọi quyền.

Phần mềm này là tài sản riêng. Việc sao chép hoặc phân phối trái phép đều bị nghiêm cấm.

---

**Được thực hiện với ❤️ bởi Trường Nguyễn**
